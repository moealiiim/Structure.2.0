# =============================================================
# Miljövariabler — KOPIERA denna fil till .env och fyll i dina värden
# Kör: cp .env.example .env   (Mac/Linux)
#      copy .env.example .env (Windows)
#
# .env får ALDRIG committeras till git!
# =============================================================

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ditt_lösenord_här
DB_NAME=company_db
